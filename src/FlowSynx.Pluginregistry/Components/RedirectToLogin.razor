@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var returnUrl = Uri.EscapeDataString(Navigation.Uri) ?? "/";
        Navigation.NavigateTo($"/api/account/login?returnUrl={returnUrl}", true);
    }
}